name: cm
description: A sample command-line application.
version: 1.0.0

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  encrypt: ^5.0.0 # or the latest version
  ansicolor: ^2.0.1 # or the latest version
  intl: ^0.17.0 
  twilio_flutter: ^0.2.0 

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
